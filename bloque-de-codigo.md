# 🧠 Uso de Bloques de Código

Cuando estás documentando código en Markdown, es muy útil mostrarlo con formato especial para que sea más legible. Esto se logra usando **bloques de código**.

## 🧱 ¿Cómo se hace?

Utiliza tres acentos graves (backticks) para abrir y cerrar el bloque de código. Puedes especificar el lenguaje para que el visor resalte la sintaxis correctamente.

### 📌 Sintaxis básica:

```markdown
```lenguaje
// Tu código aquí
```

### 🧪 Ejemplo en Python:

```python
def saludo():
    print("Hola, Markdown!")
```

### 🧪 Ejemplo en Bash:

```bash
echo "Hola mundo"
```

## 🧾 Lenguajes comunes soportados para resaltado de sintaxis

Aquí tienes una lista de 20 lenguajes comúnmente soportados por visores Markdown que permiten resaltado de sintaxis:

1. `bash`
2. `c`
3. `cpp` (C++)
4. `csharp` (C#)
5. `css`
6. `vbnet` ó `vb`
7. `go`
8. `html`
9. `java`
10. `javascript`
11. `json`
12. `kotlin`
13. `markdown`
14. `php`
15. `python`
16. `ruby`
17. `rust`
18. `sql`
19. `typescript`
20. `yaml`

> ⚠️ *El soporte puede variar según el visor Markdown que estés usando (por ejemplo, GitHub, VS Code, Typora, etc.).*

¡Usa bloques de código para que tu documentación técnica sea más clara y profesional!
